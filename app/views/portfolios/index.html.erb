<% if notice %>
  <div class="flash-message"><%= notice %></div>
<% end %>

<article>
  <div class="article-header">
    <h2>Overview of Portfolios</h2>
  </div>

  <%= render "portfolio", portfolios: @portfolios %>
</article>

<%= render "form", portfolio: @portfolio, title: "Add Portfolio" %>