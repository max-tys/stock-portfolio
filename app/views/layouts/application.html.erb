<!DOCTYPE html>
<html>
  <head>
    <title>Stock Portfolio</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Merriweather&family=Nunito&display=swap" rel="stylesheet">
    <%= stylesheet_link_tag "reset" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav>
      <div class="flex-container">
        <%= image_tag("bull-market.png", :id => "logo-img") %>
        <span id="logo-text">Stock Portfolio</span>
      </div>

      <ul>
        <li>
          <%= link_to "Overview" , portfolios_path %>
        </li>
        <li>
          <%= link_to "New Portfolio", new_portfolio_path %>
        </li>
        <li>Logout</li>
      </ul>
    </nav>

    <main>
      <%= yield %>
    </main>
  </body>
</html>
