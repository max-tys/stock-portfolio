<%= render "transactions/form", holding: @holding, transaction: @transaction, title: "Editing Transaction", model: @transaction %>

<%# button_to "Delete Transaction", @transaction, method: :delete, data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete this transaction?" } %>

<form action="<%= transaction_path(@transaction) %>" method="delete">
  <button class="button btn-delete" data-turbo-method="delete" data-turbo-confirm="Are you sure you want to delete this transaction?">
    <%= image_tag "delete.png", class: "btn-icon" %>
    Delete Transaction
  </button>
</form>
